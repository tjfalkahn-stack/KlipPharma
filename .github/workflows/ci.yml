name: CI

on:
  pull_request:
  push:
    branches: [main, "cursor/**"]

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: klippharma
          POSTGRES_PASSWORD: klippharma
          POSTGRES_DB: klippharma_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgres://klippharma:klippharma@localhost:5432/klippharma_test
      CAMPAIGN_TEST_DATABASE_URL: postgres://klippharma:klippharma@localhost:5432/klippharma_test
      PGSSLMODE: disable
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Syntax check
        run: npm run check:syntax
      - name: Lint
        run: npm run lint
      - name: Typecheck
        run: npm run typecheck
      - name: Unit, HTTP, and PostgreSQL tests
        run: npm test
      - name: Production validation
        run: npm run validate:production
